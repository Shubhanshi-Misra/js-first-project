"use strict"; // treat all JS code as a newer version of JS(wase by default hi treat hota hai but yeh acha syntax hai use karne ko)

//alert("hello") or alert( can be use for arithmatic operations)

//alert(3+3) // we are using node js not browser

// console.log(3+3)  console.log("shubhu")  [avoid these types of writing code lines use next line]

// code readibility should be high

//documentation to be prefer -- tc39 and mdn

let name = "shubhu" // string data type
let age = 3 // number values
let isLoggedIn = false // boolean data types


           // PRIMITIVE DATA TYPES--
//number  => 2 to power 53
// bigint --> number  bht zyada bada ho tab use karo specially -- trading , market ,fb
// string => " "( can be in single quote but prefer double quotes )
// boolean => true/false (very imp)
 /*null=> standalone value(representation of khali (0) value eg --  agar koi server se request  
              bheji temperature batao or server nahi bhej para toh uss samay null  prefer kare kyuki temp toh abhi aaya hi nahi)
*/
// undefined => eg of accountState in previous file
// symbol--- (used in react ) used for unique


                //OBJECT

console.log(typeof "shubhu");
//typeof null is -->object (imp ask in interview)
//typeof undefined is --> undefined  (imp ask in interview)

